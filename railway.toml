[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && python flask_server.py"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "always"

[[services]]
name = "backend"